import * as React from 'react';

export interface IListPageProps {
}

export default function ListPage (props: IListPageProps) {
  return (
    <div>
      List page, all books are here
    </div>
  );
}
